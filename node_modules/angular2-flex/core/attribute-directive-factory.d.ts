import { ClassDefinition, Type } from '@angular/core';
export declare class AttributeDirectiveFactory {
    generateDirective(name: string, size: string, getValue?: Function): Type<any>;
    generateClassDefinition(attribute: string, getValue?: Function): ClassDefinition;
}
